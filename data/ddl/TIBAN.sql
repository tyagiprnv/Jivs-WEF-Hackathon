CREATE TABLE TIBAN (
    MANDT INT,
    BANKS VARCHAR(255),
    BANKL INT,
    BANKN INT,
    BKONT VARCHAR(255),
    IBAN VARCHAR(255),
    VALID_FROM INT,
    ERNAM VARCHAR(255),
    ERDAT INT,
    TABNAME VARCHAR(255),
    TABKEY VARCHAR(255)
);